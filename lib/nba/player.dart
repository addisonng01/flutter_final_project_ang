class Player {
  final int id;
  final String first_name; // eg. Lebron
  final String last_name; // eg. James
  final String position; // eg. F

  Player({
    required this.id,
    required this.first_name,
    required this.last_name,
    required this.position,
  });
}